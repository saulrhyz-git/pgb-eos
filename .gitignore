node_modules/
dist/
.env
*.log
.DS_Store

# TypeScript project-reference build output (tsc -b emits these next to
# vite.config.ts because client/tsconfig.node.json has composite: true) —
# harmless locally but shouldn't be committed; regenerated by `npm run build`.
*.tsbuildinfo
client/vite.config.js
client/vite.config.d.ts
